#include "main.h"

/**
 * _isupper - Checks if a charater is uppercase or not
 * @c: character to be tested
 * Return: 1 if digit, else 0
 */
int _isupper(int c)
{
	if ((c >= 'A') && (c <= 'Z')
	{
		return (1);
	}

	return (0);
}
